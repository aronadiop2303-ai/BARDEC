auto-install-peers=false
strict-peer-dependencies=false

# Required for EAS cloud builders and React Native / Metro compatibility.
# pnpm's default isolated linker creates symlinks that break native module
# resolution on EAS ubuntu workers. Hoisted mode creates a flat node_modules
# tree identical to npm, which Gradle and Metro can traverse without issues.
node-linker=hoisted
